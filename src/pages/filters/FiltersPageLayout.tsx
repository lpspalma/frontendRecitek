import React from 'react';
import { Outlet } from 'react-router-dom';

type Props = {};

const FiltersPageLayout = (props: Props) => {
  return (
    <><Outlet /></>
  );
};

export default FiltersPageLayout;